{
  "name": "@axionax/sdk",
  "version": "1.9.0",
  "private": true,
  "description": "TypeScript SDK for Axionax Protocol",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "pnpm --dir ../.. exec eslint packages/sdk/src --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@axionax/blockchain-utils": "workspace:*",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.8",
    "eslint": "^8.57.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
