'use client';

import React from 'react';

/** Theme toggle removed: app uses dark theme only. This component renders nothing. */
export default function ThemeToggle(): React.JSX.Element | null {
  return null;
}
